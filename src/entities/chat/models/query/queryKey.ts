export const CHAT_QUERY_KEY = {
  chats: ["chats"],
  // chatSession: (chatSessionId: string) => ["chats", chatSessionId],
  // chatSessionTitle: (chatSessionId: string) => [
  //   "chats",
  //   "title",
  //   chatSessionId,
  // ],
} as const;
